# PRD Archive

Completed/superseded PRD task files. Archived here instead of deleted
(PRD lifecycle rule: git is the source of truth for completion status,
but archived files keep the work history discoverable).

| File | Origin | Notes |
|------|--------|-------|
| prd.json | tasks/prd.json | Base PRD task set |
| prd-cycle2.json … prd-cycle12.json | tasks/ | Ralph-TUI execution cycles 2–12 |

Restored from a 7-week-old uncommitted deletion (2026-05-16 working-tree
recovery) and archived rather than dropped. Recover any cycle by copying
the file back to `tasks/`.
