{
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; git diff --quiet HEAD^ HEAD -- . ':!*.md' ':!*.txt' ':!.github' ':!**/*.test.ts' ':!**/*.test.tsx' ':!**/*.spec.ts' ':!.env.example' ':!docs'",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
