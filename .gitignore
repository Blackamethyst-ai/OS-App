# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (secrets)
.env
.env.local
.env.*.local

# Claude session files (local workflow)
SESSION_LOG.md
CURRENT_TASK.md
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
coverage/
supabase/.temp/
.vercel

# TypeScript incremental build cache — machine-local, regenerated by tsc
tsconfig.tsbuildinfo
.env*.local

# Cinema pipeline — large/generated/personal binary assets (local + external storage only)
public/cinema-renders/
public/anchor-library/
public/anchor-library-index.json

# Local AI context — kept on disk, not published
CLAUDE.md

# Hardened 2026-07-20 after INC-2026-07-20-01: catch .env backups/copies too
.env*
!.env.example

# Incident reports / appeal documents — local only, never publish (repo is public)
.security/
